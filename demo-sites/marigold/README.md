# Marigold Bakery demo site

This small workspace proves the Snoball → Codex editing boundary. The dashboard preview reads `content.json`; Codex can edit this directory with workspace-write permissions, but publishing remains a separate reviewed action.
